predictors = [
              'repetition', 
              # 'retraction', 
              'unfilled_pause', 
              # 'unfilled_pause_longer', 
              # 'unfilled_pause_longest', 
            #   'nonword', # none exist
            #   'filled_pause', # none exist
              'error', 
              'nr_lines', # important
            #  'nr_words', # performance halfs
              'nr_words_per_line',
              # 'gender',
              'age_months'
              ]